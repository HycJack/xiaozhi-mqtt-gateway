[package]
name = "mqtt-protocol"
version = "0.1.0"
edition = "2021"

[dependencies]
bytes = "1.5"
log = "0.4"
anyhow = "1.0"
thiserror = "1.0"
tokio = { version = "1.35", features = ["io-util", "rt", "sync"] }
tokio-util = { version = "0.7", features = ["codec"] }
